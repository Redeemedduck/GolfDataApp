# ==============================================================================
# Docker Environment Variables Template
# ==============================================================================
# Copy this file to .env and fill in your actual credentials
#
# IMPORTANT:
# - Never commit .env to version control
# - Keep these secrets secure
# - For Docker, all paths should be relative to /app inside the container
# ==============================================================================

# ------------------------------------------------------------------------------
# Supabase Configuration (for cloud database sync)
# ------------------------------------------------------------------------------
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_KEY=your-anon-key-here

# ------------------------------------------------------------------------------
# AI Service Keys (for Gemini and Anthropic Claude analysis)
# ------------------------------------------------------------------------------
GEMINI_API_KEY=AIza...
ANTHROPIC_API_KEY=sk-ant-api...

# ------------------------------------------------------------------------------
# Google Cloud Platform (for BigQuery data warehouse)
# ------------------------------------------------------------------------------
GCP_PROJECT_ID=your-project-id
GCP_REGION=us-central1
BQ_DATASET_ID=golf_data
BQ_TABLE_ID=shots

# ------------------------------------------------------------------------------
# Optional: Google Application Credentials
# ------------------------------------------------------------------------------
# If using service account JSON file, mount it as a volume and set path:
# GOOGLE_APPLICATION_CREDENTIALS=/app/credentials/service-account.json
#
# In docker-compose.yml, add volume:
# volumes:
#   - ./credentials:/app/credentials:ro

# ------------------------------------------------------------------------------
# Streamlit Configuration (optional overrides)
# ------------------------------------------------------------------------------
# These are set in the Dockerfile but can be overridden here:
# STREAMLIT_SERVER_PORT=8501
# STREAMLIT_SERVER_ADDRESS=0.0.0.0
# STREAMLIT_SERVER_HEADLESS=true
# STREAMLIT_BROWSER_GATHER_USAGE_STATS=false

# ------------------------------------------------------------------------------
# Database Configuration (for SQLite in container)
# ------------------------------------------------------------------------------
# SQLite database is stored in /app/data/ (mounted volume)
# No configuration needed - automatically created on first run
