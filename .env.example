# AI Service Keys (for interactive coaching in Streamlit app)
ANTHROPIC_API_KEY=sk-ant-api...           # Claude AI (conversational coaching)
GEMINI_API_KEY=AIza...                    # Gemini AI (code execution and data analysis)

# Google Cloud Configuration (for BigQuery cloud analytics - optional)
GCP_PROJECT_ID=your-project-id
GCP_REGION=us-central1
BQ_DATASET_ID=golf_data
BQ_TABLE_ID=shots

# Optional: Path to service account JSON (if not using gcloud auth)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/credentials.json
